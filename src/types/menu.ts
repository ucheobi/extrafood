export interface IMenu {
    name: string;
    description: string;
    price: number;
    imageUrl?: string;
}

export interface IMenuCategory {
    title: string;
}

export interface IMenuImageUrl {
    url: string;
}

